{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.mjs",
    "setup": "bun run tsx src/create-admin.ts"
  },
  "dependencies": {
    "@PressScopeBd/auth": "workspace:*",
    "@PressScopeBd/db": "workspace:*",
    "@PressScopeBd/env": "workspace:*",
    "@hono/node-server": "^1.14.4",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "^4.8.2",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@PressScopeBd/config": "workspace:*",
    "@types/node": "catalog:",
    "tsdown": "^0.21.9",
    "tsx": "^4.21.0",
    "typescript": "catalog:"
  }
}
