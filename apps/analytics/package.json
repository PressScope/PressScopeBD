{
  "name": "analytics",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "go build -o ./bin/server ./cmd/server",
    "dev": "go run ./cmd/server",
    "worker": "go run ./cmd/motherduck",
    "test": "go test ./...",
    "lint": "golangci-lint run ./...",
    "check-types": "go build ./..."
  }
}
